  - if user_signed_in?
    .col-2.sidebarWrap
      .position-sticky.pt-3
        nav.sidebar.d-flex.flex-column
          ul.nav.nav-pills.flex-column.mb-auto
            li
              = link_to posts_path, class: 'nav-link link-light' do
                i.bi.bi-house-door.me-3
                | ホーム
            li
              a.nav-link.link-light href="#"
                i.bi.bi-search.me-3
                | 話題を検索
            li
              a.nav-link.link-light href="#"
                i.bi.bi-bell.me-3
                | 通知
            li
              a.nav-link.link-light href="#"
                i.bi.bi-envelope.me-3
                | メッセージ
            li
              a.nav-link.link-light href="#"
                i.bi.bi-bookmark.me-3
                | ブックマーク
            li
              = link_to user_path(current_user), class: 'nav-link link-light' do
                i.bi.bi-person.me-3
                | プロフィール
            li
              a.nav-link.link-light href="#"
                i.bi.bi-three-dots.me-3
                | もっと見る
          .d-grid.gap-2
            = button_tag "Post", type: "button", class: "postBtn btn btn-primary", data: { bs_toggle: "modal", bs_target: "#exampleModal" }
          .d-flex.align-items-start.userProfile
            .userProfileImg.me-2
              - if @user.avatar_image.attached?
                = image_tag @user.avatar_image, class: "rounded-circle", width: 40, height: 40, alt: 'Profile picture'
              - else
                = image_tag 'default.webp', class: "rounded-circle", width: 40, height: 40, alt: 'Default profile picture'
            .flex-grow-1
              h5.card-title.mb-0.fw-bold
                - if @user.name.present?
                  = @user.name
                - else 
                  = @user.user_name
              small.text-muted
                | @
                = @user.user_name
